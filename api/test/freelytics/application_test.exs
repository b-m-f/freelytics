defmodule Freelytics.ApplicationTest do
  use ExUnit.Case, async: true

  test "make sure that last_day_count_shift is always initiated on app startup" do
    # TODO: add test here, figure out how to start up the whole app with fake DB
  end
end
